<h2><%= @list.title %></h2>

<%= form_tag("/words/search", method: "get", remote: true) do %>
  <%= label_tag(:term, "Search for:") %>
  <%= text_field_tag(:term) %>
  <%= submit_tag("Search") %>
<% end %>

<div id="word-results"></div>



<%= button_to "Add word", edit_list_path(@list), :class => "button", :id => "add", remote: true %>

<div id="list-words-display">
  <ul>
    <% @list.words.each do |word| %>
    <div class="show-word">
    <p class="kanji"><strong><%= word.kanji %></strong></p>
    <p><%= word.kana %></p>
    <% word.meanings.each do |meaning| %>
      <p><%= meaning %></p>
    <% end %>
    <!-- add a delete button -->
    </div>
    <% end %>
    </ul>

  </ul>
</div>
